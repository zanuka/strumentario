# strumentario
Strumentario — the instrumentarium for agentic MCP servers. CLI + remote MCP toolkit that scaffolds, configures, and ships production-ready Model Context Protocol servers, React widgets, and AI SDK surfaces from a single declarative core.
